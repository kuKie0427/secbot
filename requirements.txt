# 构建兼容：llvmlite/numba 与 setuptools 70+ 的 spawn(dry_run) 不兼容，约束 setuptools
setuptools>=78.1.1,<70

# 核心AI和智能体框架
langchain>=0.1.0
langchain-community>=0.0.20
langchain-core>=0.1.23
langchain-experimental>=0.0.50  # 实验性功能，包含更多 Agent 类型
langchain-ollama>=0.1.0  # Ollama 集成（替代已弃用的 langchain_community.chat_models.ChatOllama）

# 异步和并发
aiohttp>=3.9.1
httpx>=0.26.0

# CLI框架
typer>=0.9.0
rich>=13.7.0
prompt-toolkit>=3.0.0

# 数据处理
pydantic>=2.5.3
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
numpy>=1.24.0

# 工具和工具调用
requests>=2.31.0
beautifulsoup4>=4.12.2
selenium>=4.17.0
playwright>=1.41.0

# 向量数据库和嵌入
# 使用Ollama进行嵌入，只需要向量存储
chromadb>=0.4.22
faiss-cpu>=1.7.4

# 记忆和状态管理
redis>=5.0.1
sqlalchemy>=2.0.25

# 系统控制
psutil>=5.9.0
distro>=1.8.0

# 日志和监控
loguru>=0.7.2

# 测试
pytest>=8.0.0
pytest-asyncio>=0.23.3

# 其他工具
python-multipart>=0.0.6
websockets>=12.0
tiktoken>=0.5.2

# 语音处理
# 注意：Whisper模型在首次使用时自动下载（base模型约150MB）
# TTS使用gTTS（在线）或pyttsx3（系统TTS），无需下载模型
openai-whisper>=20231117  # STT: 语音转文字（Whisper模型自动下载）
gtts>=2.5.0  # TTS: 文字转语音（Google TTS，需要网络）
pyttsx3>=2.90  # TTS: 文字转语音（本地系统TTS，无需网络）
pydub>=0.25.1  # 音频处理工具

# 远程控制
paramiko>=3.4.0
pywinrm>=0.4.3

# OSINT 情报
shodan>=1.31.0  # Shodan API（需 API Key）
vt-py>=0.18.0  # VirusTotal API（需 API Key）

# 搜索
duckduckgo-search>=6.0.0  # DuckDuckGo 搜索（免费）

# 协议探测
impacket>=0.12.0  # SMB/LDAP/Kerberos 等协议
pysnmp>=6.0.0  # SNMP 协议查询

# 网络扫描
scapy>=2.5.0  # ARP 扫描等底层网络操作

# FastAPI 路由层
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
sse-starlette>=2.0.0

# 云安全（可选）
# boto3>=1.34.0  # AWS S3 操作（可选，不安装时使用 HTTP 方式）